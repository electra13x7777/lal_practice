; Bakumatsu Chapter

; NINJA START (40, 35)
    lda #$28    ; Load A with X cord
    sta $1654   ; Store contents of A in addr
    lda #$23    ; Load A with Y cord
    sta $163C   ; Store contents of A in addr
; JAIL (32, 39)
    lda #$20    ; Load A with X cord
    sta $1654   ; Store contents of A in addr
    lda #$27    ; Load A with Y cord
    sta $163C   ; Store contents of A in addr
; B3 A1

; B3 A2

; KARAKURI

; MONKS

; MUSASHI

; YODO KUN

; ODE IOU

; GAMAHEBI